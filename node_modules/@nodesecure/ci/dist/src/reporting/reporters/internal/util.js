export function invertRecord(obj) {
    const invertedEntries = Object.entries(obj).map(([key, value]) => [
        value,
        key
    ]);
    return Object.fromEntries(invertedEntries);
}
//# sourceMappingURL=util.js.map