import JSXray from "@nodesecure/js-x-ray";
export declare class IgnorePatterns {
    warnings: IgnoreWarningsPatterns;
    constructor(warnings?: WarningEntries);
    static default(): IgnorePatterns;
}
export declare type WarningEntries = Partial<Record<JSXray.WarningName, string[]>>;
export declare class IgnoreWarningsPatterns {
    entries: WarningEntries;
    constructor(entries?: WarningEntries);
    has(warning: JSXray.WarningName, pkg: string): boolean;
}
export declare const kIgnoreFileName = ".nodesecureignore";
export declare function validateIgnoreFile(ignoreFile: string): {
    isValid: boolean;
    error?: string;
};
