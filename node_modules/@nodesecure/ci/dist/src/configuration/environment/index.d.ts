import { ValueOf } from "../../types";
import { Nsci } from "../standard/index.js";
declare type LockFile = ValueOf<typeof kLockFiles>;
export declare type EnvironmentContext = {
    lockFile: {
        current: LockFile;
        multiple: boolean;
    };
    compatibleStrategy: Nsci.OutputStrategy;
};
declare const kLockFiles: {
    readonly packageLock: "package-lock.json";
    readonly yarnLock: "yarn.lock";
    readonly shrinkwrap: "npm-shrinkwrap.json";
    readonly none: "none";
};
export declare function analyzeEnvironmentContext({ rootDir, strategy }: Nsci.Configuration): Promise<EnvironmentContext>;
export {};
