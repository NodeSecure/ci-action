import { ValueOf } from "../lib/types";
import * as RC from "../nodesecurerc.js";
declare type LockFile = ValueOf<typeof lockFiles>;
export declare type EnvironmentContext = {
    lockFile: {
        current: LockFile;
        multiple: boolean;
    };
    compatibleStrategy: RC.OutputStrategy;
};
declare const lockFiles: {
    readonly packageLock: "package-lock.json";
    readonly yarnLock: "yarn.lock";
    readonly shrinkwrap: "npm-shrinkwrap.json";
    readonly none: "none";
};
export declare function analyzeEnvironmentContext({ rootDir, strategy }: RC.Configuration): Promise<EnvironmentContext>;
export {};
